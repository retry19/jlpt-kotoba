# Agent Instructions

- Always commit completed changes.
- Use a Conventional Commits message for every commit (for example, `feat: add study filters` or `fix: preserve card progress`).
- After finishing an implementation, update `CHANGELOG.md`.
